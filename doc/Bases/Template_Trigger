* Шаблоны для триггеров
* Первая строка - название шаблона.
* Любые параметры могут отсутствовать, тогда они будут заполнены 0 или пустой строкой.
* Если какой-то параметр пустой, то не указывайте его вообще!
* Строки обязательно должны оканчиваться тильдой (~).
* Битвекторы считаются как строки, поэтому тоже должны оканчиваться тильдой.
#По умолчанию~
Name: ~
Model: 0
Flags: ~
NumParam: 0
StrParam: ~
